#include<stdio.h>
int main(){
    int num, temp=1,s=1;
    printf("Enter number\n");
    scanf("%d",&num);
    while(s<num/2){
        if(num%s==0)
    }
    if(temp)
        printf("The number is a power of 2");
    else    
        printf("Number is not a power of 2");
    return 0;
}